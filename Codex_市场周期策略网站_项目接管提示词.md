# Codex 项目接管与初始化指令

## 一、你的角色

你现在是本项目的长期维护者、前端工程师、Node.js 工程师、金融策略信息架构设计者和质量负责人。

你的任务不是重新从零生成一个相似网站，而是：

1. 完整接管现有代码和文档；
2. 先核实当前项目真实状态；
3. 建立可持续维护的项目基线；
4. 保留已经确认的产品边界；
5. 在后续对话中根据用户指令继续迭代；
6. 所有修改都必须可验证、可回退、可追踪。

不要机械执行一句话修改。每次开发前先检查现有实现、相关数据结构、响应式影响和潜在回归，再实施修改。

---

# 二、项目名称

**市场周期与仓位期权策略网站**

当前版本基线：

```text
Market Cycle Strategy v0.1
```

项目定位：

> 一个个人使用的市场阶段识别、仓位管理和期权策略查询网站。用户先在首页观察固定的市场周期图和多个辅助指标，再点击某个市场阶段，进入详情页查看对应的标的选择、仓位允许区间、期权策略、执行动作、潜在收益和潜在亏损。

该网站目前是静态 UI 原型，不接入真实市场数据，不连接券商，也不执行交易。

---

# 三、接管前提

用户会将现有项目源码放入当前 Codex 项目目录，可能是以下形式之一：

1. 已经解压后的完整源码目录；
2. `market-cycle-strategy-site-v0.1.zip` 压缩包；
3. 压缩包与部分解压文件同时存在。

你必须先检查当前目录，再决定如何处理。

## 目录处理原则

- 不要在项目根目录内重复套娃出多层同名目录。
- 如果只有 ZIP，先解压到临时目录，检查结构后再放到项目根目录。
- 如果源码已经存在，不要再次覆盖。
- 如果同时存在 ZIP 和源码，以实际源码为工作目录，ZIP 只作为原始备份。
- 不要删除原始压缩包，除非用户明确要求。
- 不要删除未知文件。
- 不要覆盖用户已经新增的文件。
- 不要未经确认移动项目到其他磁盘位置。

---

# 四、已经确认并冻结的产品需求

以下内容是用户已经确认的第一版边界。除非用户后续明确修改，否则不得擅自改变。

## 4.1 市场阶段

固定使用以下 9 种市场形态：

1. 高位震荡
2. 震荡下跌
3. 单边下跌
4. 恐慌暴跌
5. 底部震荡
6. 右侧反转
7. 单边上涨
8. 震荡上涨
9. 加速上涨／过热

要求：

- 首页使用横向市场周期曲线。
- 不绘制成圆形周期图。
- 每个阶段是独立可点击区域。
- 点击后进入该阶段的策略详情。
- 不得擅自合并、删除或新增市场阶段。

## 4.2 首页指标卡片

第一版固定展示 6 个指标：

1. PE
2. 前瞻 PE
3. VIX
4. VXN
5. 恐慌贪婪指数
6. 基金经理仓位指数

每张卡片独立支持以下时间范围：

```text
1个月、3个月、6个月、1年、3年、5年、10年
```

要求：

- 各卡片独立切换时间范围。
- 不做全局时间联动。
- 第一版使用静态或模拟数据。
- 不调用真实行情接口。
- 后续可以增加指标，因此数据结构必须具备扩展性。

## 4.3 仓位管理

详情页至少展示：

- QQQ
- SOXX
- SOXL
- 现金／SGOV

仓位采用独立允许区间，例如：

```text
QQQ：50%–70%
SOXX：10%–25%
SOXL：0%–5%
现金／SGOV：10%–30%
```

业务规则：

- 各资产区间相互独立。
- 不要求所有区间上下限相加等于 100%。
- 第一版不录入用户真实持仓。
- 第一版不计算实际仓位与目标仓位差额。
- 第一版不提供自动调仓。
- 必须说明每种资产在该阶段的角色、增配条件、减配条件、禁止条件和主要风险。

## 4.4 期权策略

第一版固定使用以下 8 种期权策略：

1. Protective Put
2. Collar
3. Put Debit Spread
4. Put Credit Spread
5. Covered Call
6. Cash-Secured Put
7. Call Debit Spread
8. Long Call

不得擅自增加其他复杂期权策略。

每种期权至少必须说明：

- 中文名称与英文名称；
- 策略目的；
- 属于保护、进攻还是收益增强；
- 买入和卖出的具体结构；
- 适用的市场判断；
- 适用标的；
- 到期期限选择逻辑；
- 行权价选择逻辑；
- 最大收益；
- 最大亏损；
- 盈亏平衡点；
- 上涨、横盘、下跌时的表现；
- 时间价值影响；
- 隐含波动率影响；
- 提前退出条件；
- 到期处理方式；
- 主要风险和机会成本。

第一版不接入真实期权链，最大收益、最大亏损和盈亏平衡点使用公式及示例表达。

## 4.5 市场阶段详情页

每个市场阶段使用统一的详情页模板，至少包含：

1. 阶段摘要；
2. 市场状态定义；
3. 典型识别条件；
4. 与前后相邻阶段的区别；
5. QQQ、SOXX、SOXL、现金／SGOV 的独立仓位区间；
6. 各标的的配置逻辑；
7. 适合、可选、谨慎、不建议的期权策略；
8. 初始执行动作；
9. 市场继续恶化时的动作；
10. 市场保持不变时的动作；
11. 市场改善时的动作；
12. 阶段切换条件；
13. 潜在收益来源；
14. 潜在亏损来源；
15. 最大不利情景；
16. 策略失效条件；
17. 机会成本；
18. 风险提示。

不要只写“风险较高”“建议谨慎”这类空泛内容。需要说明风险来自哪里，以及使用策略后可能失去什么、承担什么。

---

# 五、第一版明确不开发的内容

以下功能当前属于排除项，不要自行实现：

- 真实行情数据；
- 自动识别或自动判断市场阶段；
- 模拟指标输入面板；
- 市场阶段规则引擎；
- 系统建议阶段；
- 人工确认状态保存；
- 每日策略记录；
- 用户登录；
- 多用户系统；
- 数据库；
- IBKR 接口；
- 自动读取账户持仓；
- 自动下单；
- 自动回测；
- 推送通知；
- 后台内容编辑系统。

可以在架构上保留后续扩展空间，但不要为了“预留”而提前引入复杂框架、数据库或无用抽象。

---

# 六、视觉与交互要求

## 6.1 视觉方向

- 支持深色和浅色主题。
- 信息架构和版式可以参考专业金融网站的清晰度与数据密度。
- 动态交互可以参考高质量产品官网的平滑感。
- 不复制任何第三方网站的具体设计、图形或受保护素材。
- 页面应具有专业、克制、清晰的金融仪表盘风格。
- 信息读取优先于动画表现。

## 6.2 动画要求

允许：

- 页面和卡片轻量渐入；
- 市场周期曲线首次加载绘制；
- 阶段区域悬停高亮；
- 卡片轻微抬升；
- 指标曲线平滑切换；
- 深浅主题平滑过渡。

禁止：

- 大幅缩放；
- 长时间开场动画；
- 滚动劫持；
- 自动播放复杂背景；
- 频繁闪烁；
- 影响读取的夸张视差；
- 造成明显性能负担的持续动画。

必须兼容：

```css
@media (prefers-reduced-motion: reduce)
```

## 6.3 终端适配

必须适配：

- Windows 桌面浏览器；
- iPad 横屏与竖屏；
- iPhone。

最低检查宽度：

```text
375px
768px
1024px
1440px
```

要求：

- 整个页面不得出现非预期横向溢出。
- 市场周期主图在窄屏下可以在自己的容器内横向滚动。
- 触摸区域尺寸合理。
- 不能只依赖鼠标悬停才能获得关键信息。
- 键盘可以访问主要导航和交互控件。
- 深色与浅色主题均要有足够对比度。
- 红绿状态不能只依靠颜色表达，还要有文字、图标或标签。

---

# 七、当前技术架构

第一版默认技术栈：

- 前端：原生 HTML、CSS、JavaScript；
- 页面图形：内联 SVG；
- 路由：Hash Router；
- 数据：`public/data/*.json`；
- 服务端：Node.js 原生 HTTP 静态服务器；
- 持久化：无；
- 第三方运行依赖：无。

未经用户明确要求，不要增加：

- React、Vue、Angular；
- Vite、Webpack 等构建器；
- Express；
- 数据库；
- UI 组件库；
- 图表库；
- npm 运行依赖。

如果现有项目已经符合原生架构，继续在现有架构上维护。

---

# 八、预计项目结构

接管后先核实实际文件，不要只相信下面的结构。

```text
项目根目录
├─ public
│  ├─ index.html
│  ├─ styles.css
│  ├─ app.js
│  └─ data
│     ├─ stages.json
│     ├─ options.json
│     └─ indicators.json
├─ docs
│  └─ PROJECT_PLAN.md
├─ scripts
│  ├─ start-market-site.ps1
│  └─ open-firewall.ps1
├─ previews
├─ AGENTS.md
├─ README.md
├─ package.json
├─ server.js
└─ .gitignore
```

主要内容位置：

- 市场阶段内容：`public/data/stages.json`
- 期权策略内容：`public/data/options.json`
- 指标卡片数据：`public/data/indicators.json`
- 页面逻辑：`public/app.js`
- 页面样式：`public/styles.css`
- 服务端：`server.js`

不要把大量策略正文硬编码进 HTML。策略内容优先由 JSON 配置驱动。

---

# 九、服务器与访问要求

默认监听：

```text
0.0.0.0:48101
```

局域网目标地址：

```text
http://192.168.31.153:48101
```

还需要支持通过服务器的 ZeroTier IP 访问。

注意：

- 不得仅监听 `127.0.0.1`。
- 启动前检查 48101 端口是否已被其他程序占用。
- 如果端口已被占用，不要直接终止未知进程。
- 先报告占用进程及 PID，再给出处理建议。
- Windows 防火墙脚本只能创建与本项目相关的入站规则。
- 不要关闭 Windows 防火墙。
- 不要开放不必要端口。
- 第一版没有账号密码，安全边界依赖局域网和 ZeroTier 网络。
- 不要擅自改为公网监听或配置公网穿透。

---

# 十、Excel 策略文件的定位

用户可能同时提供一个仓位与期权策略 Excel。

该 Excel 只作为：

- 市场策略内容来源；
- 标的定位参考；
- 仓位边界参考；
- 期权用途参考；
- 执行动作参考；
- 风险校验参考。

不要：

- 把 Excel 页面逐表机械搬到网站；
- 直接按照单元格布局生成网页；
- 因为 Excel 只有较少市场状态，就删除网站已经确认的 9 种状态；
- 把 Excel 中的数字当成无需核实的绝对规则。

需要把 Excel 内容重新映射到网站的信息架构中，并在发生冲突时明确列出冲突，不要自行覆盖用户已经确认的需求。

---

# 十一、首次接管必须执行的工作

## 阶段 1：只读审计

先不要修改业务代码，完成以下检查：

1. 列出完整目录树；
2. 阅读：
   - `README.md`
   - `AGENTS.md`
   - `docs/PROJECT_PLAN.md`
   - `package.json`
   - `server.js`
   - `public/index.html`
   - `public/app.js`
   - `public/styles.css`
   - `public/data/stages.json`
   - `public/data/options.json`
   - `public/data/indicators.json`
3. 核实 9 种市场阶段是否齐全；
4. 核实 8 种期权策略是否齐全；
5. 核实 6 个指标及 7 个时间范围是否齐全；
6. 核实页面路由；
7. 核实深浅主题；
8. 核实响应式断点；
9. 核实服务器监听地址和端口；
10. 检查代码中是否存在明显死代码、占位代码、语法错误或路径错误；
11. 检查是否存在未说明的第三方依赖；
12. 检查 `.gitignore` 是否排除运行时文件和本地临时文件。

完成后先形成“接管审计结果”，不要凭文档宣称功能已经实现，必须以实际代码为准。

## 阶段 2：建立运行基线

执行项目现有检查命令。预计包括：

```powershell
npm run check
```

然后启动服务器并验证：

```text
GET /api/health
```

确认返回类似：

```json
{
  "ok": true
}
```

继续检查：

- 首页；
- 任一阶段详情页；
- 阶段对比页；
- 期权工具页；
- 指标说明页；
- 不存在的路由；
- 静态文件 MIME 类型；
- 404 行为；
- 深浅主题；
- 6 张指标卡片的独立周期切换；
- 9 个阶段点击跳转。

如果当前环境无法使用浏览器，明确区分：

- 已完成的代码级验证；
- 已完成的 HTTP 验证；
- 尚未完成的真实浏览器视觉验证。

不要虚构浏览器测试结果。

## 阶段 3：响应式与可访问性检查

至少检查：

```text
375 × 812
768 × 1024
1024 × 1366
1440 × 900
```

检查：

- 页面级横向溢出；
- 导航折叠；
- 指标卡片布局；
- 市场周期图容器滚动；
- 字号；
- 按钮触摸区域；
- 键盘焦点；
- ARIA 标签；
- 对比度；
- `prefers-reduced-motion`；
- 深色主题可读性。

发现问题时先记录，再进行小范围修复。

## 阶段 4：文档基线

在不与现有文档重复冲突的前提下，补齐或更新以下文档：

```text
docs/PROJECT_CONTEXT.md
docs/ARCHITECTURE.md
docs/DECISIONS.md
docs/TODO.md
docs/TESTING.md
docs/CHANGELOG.md
docs/HANDOFF.md
```

文档职责：

### `PROJECT_CONTEXT.md`

记录：

- 项目目的；
- 用户使用流程；
- 已确认需求；
- 排除项；
- 关键业务术语；
- 后续版本方向。

### `ARCHITECTURE.md`

记录：

- 文件结构；
- 页面路由；
- 数据加载流程；
- JSON 数据模型；
- SVG 市场周期图；
- 主题机制；
- 响应式机制；
- Node.js 服务逻辑；
- 后续真实数据接口边界。

### `DECISIONS.md`

记录已经做出的架构和产品决定，例如：

- 保持原生技术栈；
- 使用 Hash Router；
- 9 种市场阶段；
- 8 种期权策略；
- 独立仓位区间不加总；
- 第一版不做自动判断；
- 第一版不接入真实数据；
- 监听 `0.0.0.0:48101`；
- 使用 ZeroTier 访问。

每项决定应包含：

```text
日期
决定
原因
影响
未来何时可以重新评估
```

### `TODO.md`

只记录真实存在且尚未完成的事项，按照优先级分类：

```text
P0：阻塞运行或严重错误
P1：第一版质量问题
P2：内容校准
P3：后续功能
```

不要把用户明确排除的功能伪装成当前缺陷。

### `TESTING.md`

记录：

- 检查命令；
- 启动方式；
- API 检查；
- 页面检查清单；
- 响应式视口；
- Windows、iPad、iPhone 人工验收步骤；
- ZeroTier 验收步骤；
- 已知无法自动化的项目。

### `CHANGELOG.md`

从当前基线版本开始记录，不重写不存在的历史。

### `HANDOFF.md`

用于未来交接，记录：

- 当前可运行状态；
- 最近一次验证日期；
- 已知问题；
- 下一步；
- 重要文件；
- 部署注意事项。

## 阶段 5：Git 基线

先检查当前目录是否已经是 Git 仓库。

### 如果已经是 Git 仓库

- 检查当前分支；
- 检查工作树状态；
- 检查已有提交；
- 检查远程仓库；
- 不覆盖远程地址；
- 不强制推送；
- 不重写历史。

### 如果不是 Git 仓库

在代码和文档审计完成后：

```powershell
git init
git add .
git commit -m "chore: establish market strategy site v0.1 baseline"
```

提交前必须确保：

- 没有把 ZIP、运行日志、缓存、截图临时文件或系统垃圾误加入 Git；
- 没有加入密钥、账号、Token 或个人隐私数据；
- `.gitignore` 合理；
- 检查通过。

如果当前环境没有配置 Git 用户身份，报告需要配置，不要伪造身份。

---

# 十二、首次接管允许修复的内容

首次接管可以直接修复：

- 明确语法错误；
- 文件路径错误；
- 启动脚本错误；
- 端口或监听地址与文档不一致；
- JSON 无法加载；
- 主要路由失效；
- 页面级非预期横向溢出；
- 明显可访问性缺陷；
- 深浅主题无法切换；
- 健康检查接口失效；
- README 与实际启动方式明显不一致；
- 文档缺失或过时。

首次接管不要直接进行：

- 大规模重构；
- 框架迁移；
- 页面视觉完全重做；
- 策略内容大范围改写；
- 仓位数字重新定义；
- 新增真实数据；
- 新增自动判断；
- 新增 IBKR；
- 新增数据库；
- 新增用户系统。

---

# 十三、代码修改原则

1. 先定位根因，不做表面补丁堆叠。
2. 修改范围尽量小。
3. 避免重复代码，但不要为了抽象而抽象。
4. 保持 JSON 数据兼容性。
5. 数据字段新增时必须提供默认值或兼容处理。
6. 页面不能因单个 JSON 字段缺失而整体崩溃。
7. 对加载失败提供明确错误状态。
8. 所有交互兼容鼠标、触摸和键盘。
9. 不使用内联事件处理器。
10. 不使用 `eval`。
11. 不引入不必要的全局变量。
12. 服务器需要防止路径穿越。
13. 静态文件必须返回合理 MIME 类型。
14. 任何外部数据接入都必须等用户后续明确要求。
15. 不执行任何真实交易。
16. 所有金融策略页面必须保留风险边界说明。
17. 不把策略内容描述为确定收益或保证本金。

---

# 十四、每次修改后的最低验证

每次代码修改完成后至少执行：

```powershell
npm run check
```

启动服务后验证：

```text
/api/health
```

页面最低回归范围：

1. 首页；
2. 1 个上涨阶段详情；
3. 1 个下跌阶段详情；
4. 阶段对比页；
5. 期权工具页；
6. 指标说明页；
7. 深色主题；
8. 浅色主题；
9. 375px；
10. 768px；
11. 1440px。

涉及数据文件时，额外检查：

- 9 种阶段数量；
- 8 种期权数量；
- 6 个指标数量；
- 7 个时间周期；
- 所有引用 ID 均存在；
- JSON 格式有效；
- 中文标点和特殊字符没有乱码。

---

# 十五、首次接管最终汇报格式

完成接管后，用下面的结构汇报，不要只说“已经完成”。

## 1. 项目真实状态

- 项目根目录；
- 当前版本；
- 技术栈；
- Git 状态；
- 当前分支；
- 工作树状态；
- 远程仓库状态；
- 监听地址；
- 端口。

## 2. 已核实功能

逐项列出实际检查通过的功能。

## 3. 发现的问题

按严重程度分类：

```text
P0
P1
P2
P3
```

每个问题说明：

- 现象；
- 根因；
- 影响；
- 是否已修复；
- 验证方式。

## 4. 本次修改

列出：

- 修改文件；
- 修改原因；
- 影响范围；
- 是否存在兼容性变化。

## 5. 验证结果

明确区分：

- 语法检查；
- 单元或脚本检查；
- HTTP 检查；
- 浏览器检查；
- 响应式检查；
- 尚未完成的人工检查。

## 6. 文档状态

列出已经创建或更新的文档。

## 7. 下一步建议

只列出 3–5 个优先级最高的真实下一步，不要擅自开始下一阶段。

---

# 十六、当前首次任务

现在开始执行以下任务：

1. 检查当前目录并定位项目根目录；
2. 完成只读审计；
3. 运行现有检查；
4. 启动并验证网站；
5. 修复阻塞运行或明显错误；
6. 补齐长期维护文档；
7. 建立或确认 Git 基线；
8. 给出完整接管报告。

本轮目标是：

> 将现有 v0.1 原型变成一个状态清晰、可以运行、可以验证、可以持续维护的 Codex 项目基线。

本轮不要新增真实市场数据、自动判断、IBKR、数据库、登录系统或其他未确认功能。
