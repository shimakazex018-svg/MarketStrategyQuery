# Codex 长期工作规则

本文件只规定 Codex / AI Agent 在本项目中的长期工作方式。项目当前事实见 `docs/PROJECT_CONTEXT.md`，系统结构见 `docs/ARCHITECTURE.md`。

## 新任务读取顺序

每次新任务开始时，必须依次阅读：

1. `AGENTS.md`：明确工作规则和禁止事项。
2. `docs/PROJECT_CONTEXT.md`：了解项目当前事实。
3. `docs/ARCHITECTURE.md`：了解当前系统结构和数据流。
4. `docs/DECISIONS.md`：了解不能随意推翻的长期决策。
5. `docs/TODO.md`：了解任务池、优先级和状态。
6. `docs/TESTING.md`：了解当前有效的启动与验证方法。
7. `docs/HANDOFF.md`：了解最近一次任务的交接状态。

如果新任务涉及特定模块，还必须继续阅读该模块源码、对应数据文件和专项文档。不能只读上下文文档就直接修改代码。

## 修改前检查

1. 确认当前分支、HEAD、上游分支和工作树状态。
2. 检查是否存在用户未提交的改动；不得覆盖、清理或重置未知改动。
3. 阅读任务涉及的源码、测试、配置和专项文档，以仓库实际内容为准。
4. 确认任务是否触及固定业务边界、运行数据、网络访问、外部许可或安全边界。
5. 修改代码前先向用户说明需求理解、现状、风险、推荐方案、可选增强、待确认事项和执行计划。
6. 若基线、授权范围或外部状态无法确认，停止相关写操作并报告；不得猜测。

## 修改后检查

最低验证以 `docs/TESTING.md` 为准。代码、配置、数据模型或启动逻辑发生变化时，至少执行：

```powershell
npm.cmd run check
```

并根据修改范围验证 `/api/health`、相关内部 API、主要 Hash 路由、深浅主题、键盘操作和响应式视口。仅修改文档时，也必须执行 `git diff --check`、检查文档引用和 `git status`；条件允许时运行完整检查证明业务基线未受影响。

## 固定技术与业务约束

- 保持原生 HTML、CSS、JavaScript 与 Node.js 原生 HTTP 服务；未经用户明确批准，不增加框架、打包器、数据库或第三方运行依赖。
- 默认监听 `0.0.0.0:48101`；页面路由使用 Hash Router。
- 策略正文优先放在 `public/data/*.json`，不得大量硬编码进 HTML 或 JavaScript。
- 固定九种市场阶段和八种期权策略；不得擅自合并、新增或改变顺序。
- QQQ、SOXX、SOXL、现金/SGOV 的仓位区间相互独立，不要求合计为 100%。
- 不擅自增加自动阶段判断、自动仓位建议、IBKR、持仓读取、数据库、登录、回测或自动交易。
- 所有交互支持鼠标、触摸和键盘；适配 Windows、iPad、iPhone；动画兼容 `prefers-reduced-motion`。
- 真实市场数据必须先通过定义、对象、日期、机器可读性、稳定性、许可、历史覆盖和当前服务器访问门槛。

## 禁止事项

- 禁止使用 `reset --hard`、强制推送或历史重写覆盖已有工作，除非用户明确授权具体操作。
- 禁止删除、移动、提交或“整理”运行数据来制造干净状态。
- 禁止把演示数据、测试夹具或未经许可的数据描述为实时或正式数据。
- 禁止网页 DOM 抓取、绕过登录/验证码/访问控制或使用未经确认的私有接口。
- 禁止修改与当前任务无关的代码、配置、文案或视觉。
- 禁止虚构端口、接口、目录、数据库、模块、测试结果或外部许可。
- 禁止提交密钥、个人信息、`.env`、日志、缓存或机器相关运行状态。

## 运行数据保护

- 当前运行数据根目录是 `runtime-data/`，不得纳入 Git；实际结构见 `docs/PROJECT_CONTEXT.md`。
- `public/data/*.json` 是受版本控制的产品源数据，不属于运行缓存。
- 数据库、缩略图、HLS、日志、缓存和生成文件一旦未来出现，默认视为运行数据；在确认用途、备份、容量和清理规则前不得移动或删除。
- 发现疑似运行数据未被忽略时，只报告风险并提出独立处理建议；未经授权不得修改 `.gitignore` 或提交相关文件。

## Git 工作要求

1. 保持用户指定分支和基线，不直接合并或推送到 `main`，除非用户明确要求。
2. 提交前检查 `git diff --check`、修改范围和工作树；提交信息应单一、可追踪。
3. 不夹带运行数据、临时文件、依赖目录或无关重构。
4. 出现未知远程提交、冲突、无法解释的文件或测试失败时停止，不自行覆盖。
5. 只有在用户要求时才提交、推送、打标签或创建合并请求。

## 文档边界

- `AGENTS.md`：Codex 工作规则。
- `docs/PROJECT_CONTEXT.md`：当前项目事实，不记录过程。
- `docs/ARCHITECTURE.md`：当前架构，不记录历史争论。
- `docs/DECISIONS.md`：长期有效或已废弃的关键决策，必须标注状态。
- `docs/TODO.md`：未完成、已完成、延期或取消的任务状态，不写详细过程。
- `docs/TESTING.md`：当前有效的启动、测试和验收方法；旧方式如保留必须标注已废弃。
- `docs/CHANGELOG.md`：按时间追加实际历史改动。
- `docs/HANDOFF.md`：保持最新、短而可执行，可以重写，不无限堆积历史。

## 每次任务结束时的文档更新

每次任务结束时必须更新：

1. `docs/CHANGELOG.md`
2. `docs/HANDOFF.md`

如果任务完成、取消、拆分、延期或优先级变化，必须更新 `docs/TODO.md`。

如果项目当前事实变化，必须更新 `docs/PROJECT_CONTEXT.md`。

如果架构、目录、接口、模块关系或数据流变化，必须更新 `docs/ARCHITECTURE.md`。

如果做出新的长期技术选择，必须更新 `docs/DECISIONS.md`。

如果启动、测试、编译或验证方式变化，必须更新 `docs/TESTING.md`。

如果 Codex 工作规则、禁止事项或固定流程变化，必须更新 `AGENTS.md`。

`CHANGELOG.md` 使用追加记录；`HANDOFF.md` 更新为最新交接状态；其他文档只维护各自边界内的当前有效信息，避免重复。
