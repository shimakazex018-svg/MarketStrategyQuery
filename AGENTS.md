# Codex 项目约束

## 项目目标

维护一个个人市场周期与交易策略查询网站。第一版是静态原型，不接入真实行情、不进行自动判断、不接入IBKR。

## 技术约束

- 保持原生HTML/CSS/JavaScript与Node.js原生HTTP服务器。
- 未经明确要求，不增加前端框架、打包器、数据库或第三方依赖。
- 默认端口48101，默认监听0.0.0.0，以支持局域网与ZeroTier。
- 页面路由使用Hash路由，避免静态服务器重写配置。
- 策略内容优先放在`public/data/*.json`，不要硬编码到HTML。
- 所有交互必须支持鼠标、触摸和键盘。
- 必须适配Windows桌面浏览器、iPad和iPhone。
- 动画必须兼容`prefers-reduced-motion`。

## 业务约束

- 固定9种市场形态，不擅自合并或新增。
- 固定8种期权策略，不擅自新增复杂策略。
- 各资产仓位区间相互独立，不要求加总为100%。
- 第一版不显示系统建议、不保存用户确认、不读取当前实际仓位。
- 所有期权策略必须说明结构、目的、最大收益、最大亏损、盈亏平衡与主要风险。
- 不将Excel机械搬运到网页；Excel只作为策略内容参考。

## 修改后的最低验证

1. `npm run check`
2. 启动服务器并请求 `/api/health`
3. 检查首页、任一阶段详情、阶段对比、期权工具和指标说明
4. 检查375px、768px、1440px宽度下无横向溢出（市场周期图自身允许容器内横向滚动）
